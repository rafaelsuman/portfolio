# Portfolio Project

## Workflow
- Make all changes directly in this folder (`portfolio/`). Do NOT use git worktrees.
- Do NOT commit automatically. The user commits manually via VS Code.
- Always preview changes with the dev server before finishing.
